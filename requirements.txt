# Core runtime: everything here installs cleanly and runs fully offline.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9        # form parsing for the chat endpoint
scikit-learn>=1.3,<2.0         # TF-IDF retrieval (pure-Python fallback if absent)
numpy>=1.24

# Optional real LLM providers. Not required for the offline stub, tests, or `make demo`.
# Install with: pip install -r requirements-llm.txt
